{
  "name": "@scola/api-http",
  "version": "3.1.0",
  "description": "HTTP API",
  "repository": "github:scola84/node-api-http",
  "author": "M.C. van den Boogaart <scola@kvdb.name>",
  "license": "MIT",
  "main": "./dist/api-http.js",
  "jsnext:main": "index.js",
  "dependencies": {
    "@scola/error": "^4.2.0",
    "qs": "^6.3.0"
  },
  "devDependencies": {
    "rollup": "^0.37.0",
    "rollup-plugin-buble": "^0.15.0",
    "uglify-js": "^2.7.5"
  },
  "scripts": {
    "build": "npm run cleanup && npm run rollup",
    "build:min": "npm run build && npm run min",
    "cleanup": "rm -rf dist",
    "min": "uglifyjs dist/api-http.js -m -o dist/api-http.min.js",
    "prepublish": "npm run build:min",
    "rollup": "rollup -c"
  }
}
